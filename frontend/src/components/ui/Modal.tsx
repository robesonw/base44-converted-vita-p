import React from 'react';\nimport * as RadixDialog from '@radix-ui/react-dialog';\n\nconst Modal = ({ open, onOpenChange, title, description, children }) => {\n  return (\n    <RadixDialog.Root open={open} onOpenChange={onOpenChange}>\n      <RadixDialog.Overlay className="fixed inset-0 bg-black opacity-30" />\n      <RadixDialog.Content className="fixed bg-white p-6 rounded-lg w-full max-w-md mx-auto top-1/4">\n        <RadixDialog.Title className="text-lg font-medium">{title}</RadixDialog.Title>\n        <RadixDialog.Description className="text-sm text-gray-600">{description}</RadixDialog.Description>\n        {children}\n      </RadixDialog.Content>\n    </RadixDialog.Root>\n  );\n};\nexport default Modal;